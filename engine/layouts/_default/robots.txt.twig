{%- if site.geo.staging|default(false) -%}
# Staging host - not for indexing.
User-agent: *
Disallow: /
{%- else -%}
User-agent: *
Allow: /
Disallow: /404.html

Sitemap: {{ site.baseurl }}sitemap.xml
{% if page.body|default('') is not empty %}
{{ page.body|raw }}
{% endif %}
{%- endif -%}
