# Domain migration: keep DNS and certificates for this hostname.
server {
    listen 80;
    server_name en.45.140.14.7.sslip.io www.en.45.140.14.7.sslip.io;
    location ^~ /.well-known/acme-challenge/ { root /var/www/certbot; }
    location / { return 301 https://aigirlfriendranking.site$request_uri; }
}
