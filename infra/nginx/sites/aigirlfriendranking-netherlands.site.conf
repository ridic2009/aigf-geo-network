# Domain migration: keep DNS and certificates for this hostname.
server {
    listen 80;
    server_name aigirlfriendranking-netherlands.site www.aigirlfriendranking-netherlands.site;
    location ^~ /.well-known/acme-challenge/ { root /var/www/certbot; }
    location / { return 301 http://nl.45.140.14.7.sslip.io$request_uri; }
}
