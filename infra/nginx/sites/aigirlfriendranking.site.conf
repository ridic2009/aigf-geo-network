# EN — aigirlfriendranking.site
# GENERATED by scripts/nginx-config. Do not edit on the server: regenerate and
# redeploy, otherwise the next release silently overwrites your change.

server {
    listen 80;
    listen [::]:80;
    server_name aigirlfriendranking.site www.aigirlfriendranking.site;

    # Let certbot renew without taking the site down.
    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://aigirlfriendranking.site$request_uri;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;

    server_name www.aigirlfriendranking.site;

    ssl_certificate     /etc/letsencrypt/live/aigirlfriendranking.site/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/aigirlfriendranking.site/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;

    return 301 https://aigirlfriendranking.site$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;

    server_name aigirlfriendranking.site;

    ssl_certificate     /etc/letsencrypt/live/aigirlfriendranking.site/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/aigirlfriendranking.site/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    root /srv/www/aigirlfriendranking.site/current;
    index index.html;

    charset utf-8;

    include /etc/nginx/snippets/aigf-security-headers.conf;
    # ConvertStudio serves the pictures of this site under /images/en/. An
    # address from before (image search, other sites) that has no file any more
    # moves there for good; while the network publishes, the file is still there.
    # Before the cache snippet: the first regular expression that matches wins.
    location ~ "^/images/(?!en/)" {
        include /etc/nginx/snippets/aigf-security-headers.conf;
        add_header Cache-Control "public, max-age=2592000" always;
        try_files $uri @moved-image;
    }
    location @moved-image {
        rewrite ^/images/(.*)$ /images/en/$1 permanent;
    }
    include /etc/nginx/snippets/aigf-static-cache.conf;
    # Only root-owned, validated directives are included (never release-writable code).
    include /etc/nginx/aigf-redirects/aigirlfriendranking.site/*.conf;
    location = /redirects.nginx.conf { deny all; }
    location = /redirects.json { deny all; }
    location ^~ /server/ { deny all; }
    location = /_redirects { deny all; }

    # Pretty URLs: /reviews/candy-ai/ -> /reviews/candy-ai/index.html
    location / {
        try_files $uri $uri/ =404;
    }

    error_page 404 /404.html;
    location = /404.html {
        internal;
    }

    # There is no application here: nothing to probe.
    location ~ /\.(?!well-known) {
        deny all;
    }

    access_log /var/log/nginx/aigirlfriendranking.site.access.log;
    error_log  /var/log/nginx/aigirlfriendranking.site.error.log warn;
}
