# DE — de.45.140.14.7.sslip.io
# GENERATED by scripts/nginx-config --http-only. HTTP only: use it for the very
# first deploy, then re-run scripts/nginx-config without --http-only.

server {
    listen 80;
    listen [::]:80;
    server_name de.45.140.14.7.sslip.io www.de.45.140.14.7.sslip.io;

    root /srv/www/de.45.140.14.7.sslip.io/current;
    index index.html;

    charset utf-8;

    include /etc/nginx/snippets/aigf-security-headers.conf;
    include /etc/nginx/snippets/aigf-static-cache.conf;

    # Pretty URLs: /reviews/candy-ai/ -> /reviews/candy-ai/index.html
    location / {
        try_files $uri $uri/ =404;
    }

    error_page 404 /404.html;
    location = /404.html {
        internal;
    }

    # There is no application here: nothing to probe.
    location ~ /\.(?!well-known) {
        deny all;
    }

    access_log /var/log/nginx/de.45.140.14.7.sslip.io.access.log;
    error_log  /var/log/nginx/de.45.140.14.7.sslip.io.error.log warn;
}
